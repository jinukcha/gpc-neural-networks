# R0B closeout

R0B is complete. The accepted path now covers straight and curved spans, miter/bevel/profile-transition joins, terrain-stepped and retaining spans, deterministic mixed segmentation, exact source replay, exact socket alignment and complete triangle source coverage.

The following remain outside R0B and are not claimed:

```text
tower families               R0C
battlement module rhythm     R0C
gate complexes               R0D
ring assembly                R0E
collision/navigation         R0E
Godot stored-copy product    R0E
reference/genericity closeout R0F
```

R0C starts only after an explicit instruction.
