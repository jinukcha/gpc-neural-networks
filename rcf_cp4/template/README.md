# R0A-CP4 — exact runtime replay and R0A closeout

This checkpoint closes R0A without adding new fortification geometry.

Scope:

- materialize two fresh CPython 3.13.5 runtimes from the retained 58-wheel offline lock;
- replay the admitted CAD capability and the complete CP1 → CP2 → CP3 positive product path;
- require runtime A, runtime B and the accepted CP3 reference to be byte-identical;
- preserve implementation/full/patch/source checkpoints before replay;
- generate final full, cumulative fixed-baseline patch and source-delta bundles from one final tree;
- prove parent-full + source delta = final full and accepted cumulative patch + the same delta = final cumulative patch;
- close R0A only after every status remains explicit.

Non-goals:

- curved wall spans;
- joins or terrain adaptation;
- towers, gates, collision, navigation or Godot import;
- repeating CP3 negative-gate development.
